/// List in dart is ordered collection of items similar to container where we store the values in a specific order.

void main() {
  // List<int> numbers = [1, 2, 3, 4, 5, 'Hello World!'];

  // List<String> names = ['John', 'Doe', 'Jane', 'Doe'];

  List<Object?> complexList = [3, 8, 'Hello World!', null];

  // complexList[0];

  // print(complexList[0]);
  // print(complexList[1]);
  // print(complexList[2]);
  // print(complexList[3]);

  print(complexList.first);
  print(complexList.last);
}
