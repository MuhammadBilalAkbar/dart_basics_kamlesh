/// AND &&, OR ||, NOT !
void main() {
  // if (1 < 2 && 3 > 2) {
  //   print('Both expressions are true');
  // }

  // if (1 < 2 || 3 < 2) {
  //   print('At least one the expressions is true');
  // }

  if (!(1 < 2)) {
    print('Only if expression is false.');
  }
}
