/// dart:core and dart:math has a lot of cool functions to do all sorts of math stuff.

import 'dart:math';

void main() {
  /// Rounding functions 'round', 'ceil', and 'floor'.z
  /// round
  double num = 3.68767;
  // print(num.round());

  /// ceil
  // num = 3.1;
  // print(num.ceil());

  /// floor
  // num = 3.9;
  // print(num.floor());

  /// Comparison functions
  // num = 3.1;
  // double num2 = 5.6;
  // print(num.compareTo(num2));
  // print(num2.compareTo(num));
  // print(num.compareTo(num));

  /// Truncate function which is opposite of rounding
  // num = 3.456;
  // print(num.truncate());
  // num = 3.11234;
  // print(num.truncate());

  /// Other functions
  // num = -3.4;
  // print(num.abs());

  // num = 9.0;
  // print(sqrt(num));

  /// min() and max()
  // num = 3.4;
  // double num2 = 5.6;
  // print(min(num, num2));
  // print(max(num, num2));

  /// math library
  /// Constants
  // print(pi);
  // print(e);
  /// Random number generation
  // final random = Random();
  // int randomInt = random.nextInt(10);
  // print(randomInt);
  // double randomDouble = random.nextDouble();
  // print(randomDouble);
}
